spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: never
 # mvc:
  #  hiddenmethod:
   #   filter:
    #    enabled: true


cloudinary:
  api-key: test
  api-secret: test

  ##${CLOUDINARY_API_SECRET}
  cloud-name: test
#Change server port
#server.port=8000

