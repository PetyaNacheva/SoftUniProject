spring:
  sql:
    init:
      mode: never
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop

  mvc:
    hiddenmethod:
      filter:
        enabled: true

cloudinary:
  api-key: test
  api-secret: test

  ##${CLOUDINARY_API_SECRET}
  cloud-name: test
#Change server port
#server.port=8000

